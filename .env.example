# =============================================================================
# .env の見本(これはGitHubに上がる。.env 本体は上がらない)
#
#   初回だけ、これをコピーして .env を作る:
#     Windows: Copy-Item .env.example .env
#     Mac:     cp .env.example .env
#
#   ★項目を増やしたら、このファイルにも同じ項目を書き足すこと。
#     見本が古いと、他の人が起動できなくなる。
# =============================================================================

# --- アプリの設定 ---
# development = エラーが詳しく出る + HTMLの変更が即反映 / production = 本番
APP_ENV=development

# アプリが待ち受ける番号。compose.yml の "8080:8080" の右側と合わせる。
PORT=8080

# SECRET_KEY: ログイン状態をブラウザに預けるときの「割り印」。
#             漏れるとログイン状態を偽造されるので、本番では必ず変更する。
SECRET_KEY=please-change-this-to-a-long-random-string

# --- データベースの設定 ---
# DB_HOST: Dockerの中では compose.yml のサービス名 "db" がそのまま住所になる。
DB_HOST=db
DB_PORT=3306
DB_NAME=hack_app
DB_USER=hack_user
DB_PASSWORD=hack_password
DB_ROOT_PASSWORD=root_password

# --- 機能のON/OFF ---
# false にすると、ログイン関連の受付を登録しなくなる。
AUTH_ENABLED=true
